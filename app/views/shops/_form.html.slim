= simple_form_for(shop, html: {class: 'modal hide fade'}) do |f|
  .modal-header
    button type="button" class="close" data-dismiss="modal" ×
    h3= t('shops.new_shop')
  .modal-body
    = f.input :item
    = f.input :sell_amount
    = f.input :sell_price
    = f.input :sell_currency, collection: Shop::CURRENCIES
    = f.input :buy_amount
    = f.input :buy_price
    = f.input :buy_currency, collection: Shop::CURRENCIES
    = f.input :world, collection: Shop::WORLDS.map {|w| [w.capitalize, w] }
    = f.input :location_x
    = f.input :location_y
    = f.input :location_z
  .modal-footer
    = f.submit t('shops.list_shop'), class: 'btn btn-primary'
